# Plan: Run codegen

## Context
Mark the "Run codegen" roadmap item as complete — the generated stubs (`src/generated/auth.ts`, `src/generated/breath_sessions.ts`) were already committed in `4b791f1` as part of plan 02.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### Phase 1: Update roadmap

- [x] **Task 1: Mark roadmap item 5.3 "Run codegen" as complete**
  Files: `.ai-factory/ROADMAP.md`
  Change the unchecked checkbox `- [ ] **Run codegen**` under section 5.3 to `- [x] **Run codegen**`. The generated files already exist and the build compiles — no codegen run or commit is needed.
